# 🤖 AI-Assistant for Knowledge Base (VK Internship Task)

Решение профильного задания на позицию Backend/AI стажера. 
Бот автоматизирует процесс наполнения базы знаний: принимает документы, генерирует краткое содержание (summary) с помощью LLM и структурирует данные в Google Sheets.

## 🔗 Ссылки
* **Бот:** @VK_intern_2025_bot (ID: *вставь_сюда_id_из_botfather*)
* **Стек:** n8n, DeepSeek V3 (via API), Google Sheets, Telegram Bot API.

## 🛠 Архитектура решения
Реализован `ETL-пайплайн` на платформе **n8n**:

1.  **Extract (Извлечение):**
    * Telegram Trigger перехватывает документы (PDF) и текстовые сообщения.
    * Модуль `Extract from File` вычитывает текстовый слой из PDF-документов.
2.  **Transform (Трансформация):**
    * Сформированный контекст отправляется в **DeepSeek API**.
    * Использован `System Prompt` для жесткого ограничения формата ответа (JSON).
    * Ответ парсится через `JSON.stringify` для валидации спецсимволов.
3.  **Load (Загрузка):**
    * Данные (Имя файла, Саммари, Теги, Автор) сохраняются в Google Sheets.

## 📷 Демонстрация
### 1. Сценарий обработки (Workflow)
![Workflow](screenshots/workflow.png)

### 2. Результат в базе знаний
![Google Sheets](screenshots/table.png)

## 🧠 Почему такие инструменты?
* **n8n:** Выбран за наглядность визуализации потоков данных и встроенную поддержку работы с бинарными файлами, что критично для PDF.
* **DeepSeek:** Оптимальное соотношение цена/качество для задач саммаризации на русском языке.
* **JSON Mode:** В промпте использован строгий JSON-формат для предсказуемой интеграции с таблицами.

## 🚀 Future Improvements (Что улучшить)
1.  **OCR:** Добавить Tesseract для сканированных PDF (сейчас работает с текстовым слоем).
2.  **Vector DB:** Внедрить Pinecone для RAG-поиска по базе.
3.  **Error Handling:** Добавить ветвление для обработки ошибок (например, если файл поврежден).
